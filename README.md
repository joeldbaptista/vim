# A very simple `vim` configuration file

This is a simple (one file) configuration for vim. The purpose is to avoid the use of many plugins, 
and other bloating appendages.

Even the folder `colors` is perhaps too much, one color scheme should be enough.
